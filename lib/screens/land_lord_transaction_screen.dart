import 'package:flutter/material.dart';

class LandLordTransactionScreen extends StatefulWidget {
  const LandLordTransactionScreen({Key? key}) : super(key: key);

  @override
  State<LandLordTransactionScreen> createState() =>
      _LandLordTransactionScreenState();
}

class _LandLordTransactionScreenState extends State<LandLordTransactionScreen> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
